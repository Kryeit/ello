class User {
    constructor(uuid, username, creation, roles) {
        this.uuid = uuid;
        this.username = username;
        this.creation = creation;
        this.roles = roles;
    }
}

export default User;